# ai-docs — notes for AI agents (not published)

Reference material for AI agents working on the site. **Excluded from the Jekyll
build** (see `exclude:` in `_config.yaml`), so nothing here reaches the website.

- **`lab-research.md`** — source-checked notes on Sam and the lab (bio, awards,
  research themes, flagship papers) for writing site copy.

Working guidelines and template docs live in `CLAUDE.md` at the repo root.
